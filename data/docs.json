[{"title":"部署指南","description":"如何构建并部署这个站点","group":"入门","order":1,"_id":"deploy","id":"deploy","path":"/docs/deploy","body":"\r\n# 部署指南\r\n\r\n本站在构建时使用 [Pagefind](https://pagefind.app) 生成全站搜索索引。\r\n\r\n## 本地构建\r\n\r\n```bash\r\nnpm run build\r\n```\r\n\r\n构建产物输出到 `dist/`，并自动生成 `pagefind/` 搜索索引。\r\n\r\n## 本地预览\r\n\r\n```bash\r\nnpm run preview\r\n```\r\n\r\n## 部署\r\n\r\n任意支持静态文件的平台均可部署 `dist/` 目录：Vercel、Netlify、GitHub Pages、Cloudflare Pages 等。\r\n\r\n## 内容管理（Pages CMS）\r\n\r\n本站使用 [Pages CMS](https://pagescms.org) 作为内容后台——一个直接读写 GitHub 仓库的开源 CMS，无需数据库。\r\n\r\n### 启用步骤\r\n\r\n1. 将仓库托管到 GitHub（例如 `stelarith/stelarith.dev`）\r\n2. 打开 [app.pagescms.org](https://app.pagescms.org)，用 GitHub 账号登录\r\n3. 按提示安装 Pages CMS GitHub App，并授权给本仓库\r\n4. 在仓库列表中选择本站仓库\r\n5. 仓库根目录的 `.pages.yml` 已定义好所有内容集合（文章/项目/笔记/文档/友链/图库），打开即可开始编辑\r\n6. 保存后 Pages CMS 自动 commit 回仓库，触发部署平台的自动构建\r\n\r\n### 字段说明\r\n\r\n`.pages.yml` 中的字段与 `src/content.config.ts` 的 schema 一一对应：\r\n- `title` / `description` / `body`：标题、摘要、正文（富文本）\r\n- `pubDate` / `updatedDate`：发布日期 / 更新日期\r\n- `tags`：标签（`list: true` 支持多个）\r\n- `draft`：草稿开关\r\n- `layout`：`default` / `wide` 两种内容布局\r\n- 项目特有：`status`（进行中/已归档/构想中）、`repo`、`demo`\r\n","bodyHtml":"<h1>部署指南</h1>\n<p>本站在构建时使用 <a href=\"https://pagefind.app\">Pagefind</a> 生成全站搜索索引。</p>\n<h2>本地构建</h2>\n<pre><code class=\"language-bash\">npm run build\n</code></pre>\n<p>构建产物输出到 <code>dist/</code>，并自动生成 <code>pagefind/</code> 搜索索引。</p>\n<h2>本地预览</h2>\n<pre><code class=\"language-bash\">npm run preview\n</code></pre>\n<h2>部署</h2>\n<p>任意支持静态文件的平台均可部署 <code>dist/</code> 目录：Vercel、Netlify、GitHub Pages、Cloudflare Pages 等。</p>\n<h2>内容管理（Pages CMS）</h2>\n<p>本站使用 <a href=\"https://pagescms.org\">Pages CMS</a> 作为内容后台——一个直接读写 GitHub 仓库的开源 CMS，无需数据库。</p>\n<h3>启用步骤</h3>\n<ol>\n<li>将仓库托管到 GitHub（例如 <code>stelarith/stelarith.dev</code>）</li>\n<li>打开 <a href=\"https://app.pagescms.org\">app.pagescms.org</a>，用 GitHub 账号登录</li>\n<li>按提示安装 Pages CMS GitHub App，并授权给本仓库</li>\n<li>在仓库列表中选择本站仓库</li>\n<li>仓库根目录的 <code>.pages.yml</code> 已定义好所有内容集合（文章/项目/笔记/文档/友链/图库），打开即可开始编辑</li>\n<li>保存后 Pages CMS 自动 commit 回仓库，触发部署平台的自动构建</li>\n</ol>\n<h3>字段说明</h3>\n<p><code>.pages.yml</code> 中的字段与 <code>src/content.config.ts</code> 的 schema 一一对应：</p>\n<ul>\n<li><code>title</code> / <code>description</code> / <code>body</code>：标题、摘要、正文（富文本）</li>\n<li><code>pubDate</code> / <code>updatedDate</code>：发布日期 / 更新日期</li>\n<li><code>tags</code>：标签（<code>list: true</code> 支持多个）</li>\n<li><code>draft</code>：草稿开关</li>\n<li><code>layout</code>：<code>default</code> / <code>wide</code> 两种内容布局</li>\n<li>项目特有：<code>status</code>（进行中/已归档/构想中）、<code>repo</code>、<code>demo</code></li>\n</ul>\n"},{"title":"欢迎","description":"星璃文档从这里开始","group":"入门","order":0,"_id":"intro","id":"intro","path":"/docs/intro","body":"\r\n# 欢迎来到星璃的文档\r\n\r\n这里是星璃的项目文档空间，记录技术方案、使用说明与设计思路。\r\n\r\n## 如何编写文档\r\n\r\n在 `src/content/docs/` 下新建 `.md` 文件，通过 frontmatter 的 `group` 和 `order` 控制分组与排序。\r\n\r\n> 示例：这篇文章属于「入门」分组，排序 0。\r\n","bodyHtml":"<h1>欢迎来到星璃的文档</h1>\n<p>这里是星璃的项目文档空间，记录技术方案、使用说明与设计思路。</p>\n<h2>如何编写文档</h2>\n<p>在 <code>src/content/docs/</code> 下新建 <code>.md</code> 文件，通过 frontmatter 的 <code>group</code> 和 <code>order</code> 控制分组与排序。</p>\n<blockquote>\n<p>示例：这篇文章属于「入门」分组，排序 0。</p>\n</blockquote>\n"}]